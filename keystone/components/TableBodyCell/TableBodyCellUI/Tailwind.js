export const Tailwind = (props) => {
  return (
    <td
      // css={{
      //   borderBottom: `1px solid ${colors.border}`,
      //   fontSize: typography.fontSize.medium,
      // }}
      {...props}
    />
  );
};
